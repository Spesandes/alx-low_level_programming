#include <stdio.h>
/**
 * main - Prints all single digits of base 10
 * starting from 0
 * Return: Always 0 (Success)
 */
int main(void)
{
	int number;

	for (number = 0; number < 10; number++)
	{
		printf("%d\n", number);
	}
	printf("\n");
		return (0);
}
